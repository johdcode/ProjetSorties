{% extends 'base.html.twig' %}

{% block title %}Ajouter un participant | {{ parent() }}{% endblock %}

{% block body %}
    <div class="div-form">
        <br>
        <h1>Nouveau Participant</h1>
        <hr>
        {{ include('participant/_form.html.twig') }}

        <div class="row">
            <a href="{{ path('participant_index') }}" class="btn btn-secondary mx-auto">Retour à la liste</a>
        </div>
        <br>
    </div>
    <br>
{% endblock %}
